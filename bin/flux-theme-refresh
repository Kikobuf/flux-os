#!/bin/bash

# flux:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/flux/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  FLUX_THEME_SKIP_BACKGROUND=1 flux-theme-set "$(cat $THEME_NAME_PATH)"
fi
