#!/bin/bash

# flux:summary=List available monospace fonts
# flux:examples=flux font list | flux font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|flux' | sort -u
