#!/bin/bash

# flux:summary=Launch a TUI or focus an existing terminal window for it
# flux:args=<command> [args...]

APP_ID="org.flux.$(basename "$1")"
LAUNCH_COMMAND="flux-launch-tui $@"

exec flux-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
