#!/bin/bash

# flux:summary=Update user-installed git themes

for dir in ~/.config/flux/themes/*/; do
  if [[ -d $dir ]] && [[ ! -L ${dir%/} ]] && [[ -d $dir/.git ]]; then
    echo "Updating: $(basename "$dir")"
    git -C "$dir" pull
  fi
done
