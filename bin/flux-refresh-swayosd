#!/bin/bash

# flux:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Flux defaults and restart the service.

flux-refresh-config swayosd/config.toml
flux-refresh-config swayosd/style.css
flux-restart-swayosd
