#!/bin/bash

# flux:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/flux/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! flux-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
