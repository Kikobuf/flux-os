#!/bin/bash

# flux:summary=Toggle Waybar visibility
# flux:examples=flux toggle waybar

flux-toggle waybar-off

if pgrep -x waybar >/dev/null; then
  pkill -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
